#include <stdio.h>
int main() 
{
    int x,res=0;
    scanf("%d",&x);
    if(x%5==0){
        int z=x/5;
        printf("%d",z);
    }
    else if(x<=5)
    {
        printf("1");
    }
    else{
        res=(x/5)+1;
        printf("%d",res);
    }

    return 0;
}
